How to run the Frontend
Go to fronted folder and run:
```bash
npm install
npm run dev
```
This will start the development server at http://localhost:SomePort 
(usually 3000).


Make sure you have the backend server running as well to provide data to the frontend. (later)